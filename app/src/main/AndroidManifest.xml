<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whattodo">

    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- MAKE ALARMS RESTART ON REBOOT -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatToDo">
        <activity android:name=".NearestEventActivity"></activity>
        <activity android:name=".MapActivity" />

        <activity android:name=".PerfilOrganizadorActivity"
            android:parentActivityName=".menus.MenuOrganizerActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivityPopUp"
            android:label="@string/title_activity_maps_pop_up"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
            android:parentActivityName=".menus.MenuOrganizerActivity"/>

        <activity
            android:name=".TicketsListActivity"
            android:parentActivityName=".menus.MenuParticipantActivity"/>

        <activity android:name=".MapActivity" />
        <activity
            android:name=".CreateEventActivity"
            android:parentActivityName=".menus.MenuOrganizerActivity" />
        <activity
            android:name=".menus.MenuOrganizerActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".menus.OrganizerHomeActivity"
            android:parentActivityName=".menus.MenuOrganizerActivity" />
        <activity
            android:name=".menus.MenuParticipantActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".menus.ParticipantHomeActivity"
            android:parentActivityName=".menus.MenuParticipantActivity" />
        <activity
            android:name=".menus.HomeActivity"
            android:parentActivityName=".menus.MenuActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key" />
        </activity>
        <activity
            android:name=".menus.MenuActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".menus.MenuActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menus.MenuActivity" />
        </activity>
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".menus.MenuActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menus.MenuActivity" />
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>